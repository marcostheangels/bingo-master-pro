# Sessão 16/07/2026 — Bingo VIP Club

Registro da conversa para consulta futura. (Arquivo seguro para versionar; o
`HISTORICO.md` contém chaves de API reais e fica APENAS local, sem commit.)

---

## 1. Instalação de agentes de automação

- Criada a pasta `agents/` com arquitetura base (`BaseAgent.js`) + executor (`index.js`)
  e 4 agentes reutilizáveis:
  - `deploy`      -> deploy completo (valida server.js, Firebase, git commit + push)
  - `diagnostico` -> checa integridade dos JSON e do game-logic/server.js
  - `creditos`    -> consolida/sincroniza fichas e créditos admin dos jogadores
  - `backup`      -> copia os dados JSON para `backups/<timestamp>`
- Uso: `npm run agent <nome>` ou `node agents/index.js <nome>` / `list`.
- Documentado no `AGENTS.md`.

## 2. Correção do botão "Excluir Jogador" no painel admin (network.js)

Sintoma reportado: ao selecionar um jogador no select e clicar em "Excluir", a tela
travava (só voltava com F5). Depois de meia solução, relatou: "não exclui o jogador".

Causa raiz final: **cache do navegador servindo o `network.js` antigo** (o `index.html`
tinha `network.js?v=28` e o Firebase deploy não mudava a query string, então o browser
seguia usando a versão em cache que travava/quebrava).

O que foi corrigido ao longo das iterações (todas em `network.js`):
- `excluirUsuarioAdmin` / `confirmarExclusaoJogador`:
  - Adicionado `AbortController` com timeout (12s) no `fetch` do DELETE — antes não havia
    timeout e a promise podia ficar pendente travar o modal (tela congelada).
  - `finalizar` sempre fecha o modal e reabilita os botões (nunca mais trava a tela).
  - Clique fora do modal (no overlay) fecha o modal.
  - CPF sempre enviado limpo (`replace(/\D/g,'')`) para bater com o backend.
  - Nome recuperado com fallback (`dataset.nome` -> `textContent`).
  - Remoção imediata da `<option>` do select assim que o servidor confirma sucesso.
  - `try/catch` em volta de todo o `confirmar` + `alert()` de fallback (nenhum erro
    passa despercebido).
  - Toasts visíveis em cada etapa + `console.log('[EXCLUIR] ...')` para diagnóstico.
- `agents/DiagnosticoAgent.js`: corrigido o checker de `setChips` (procurava em
  `game-logic.js`, mas a função `setChips` fica em `server.js:357`). Agora verifica
  `adminUpdateChips`/`sendToHost` em game-logic e `setChips`/`creditarFichas` em server.js.
- `index.html`: bump de versão `game-logic.js?v=30 -> v31` e `network.js?v=28 -> v29`
  (esta foi a correção definitiva — força o browser a baixar o arquivo novo).

### Verificação feita
- Backend testado ao vivo contra o Render (`https://bingo-master-pro-fcty.onrender.com`)
  com a senha mestra: rota `DELETE /api/admin/usuario/excluir` retorna 200 e remove o
  usuário. Preflight OPTIONS (CORS) retorna 204. Backend 100% funcional.
- Sintaxe validada (`node --check network.js`, `node -c server.js`).

### Resultado
Após o deploy com bump de versão, o usuário confirmou: "agora funcionou deu certinho".

---

## 3. Corrupção de encoding do index.html (PowerShell)

Ao testar o bump de versão via PowerShell (`Set-Content`/`Get-Content` sem `-Encoding utf8`),
o `index.html` foi gravado em ANSI e todos os acentos viraram `?` (ex: `Cr?dito`, `B?nus`).
Isso quebrava a renderização e o site parou de funcionar.

Correção:
- Restaurado o `index.html` íntegro a partir de `backups/2026-07-16_12-11-21/index.html`
  (validado byte-a-byte: BOM UTF-8 + acentos presentes).
- `deploy-all.ps1` corrigido para sempre gravar com `-Encoding utf8`.
- Commit `a195024` (íntegro) + deploy `74fa6c5`.

## 4. "Não exclui e não aparece nada" — causa: cache do Service Worker

Após os deploys, o usuário reportou: seleciona o jogador, clica em Excluir e NÃO aparece nada
(nem modal, nem toast). O backend foi testado ao vivo (DELETE retorna 200 + success) e o
frontend foi testado com **jsdom** (instalado via `npm i jsdom`): o fluxo
`confirmarExclusaoJogador` -> `excluirUsuarioAdmin` -> modal abre (`display=flex`) funciona
perfeitamente no código atual.

Causa raiz: o `sw.js` tinha `CACHE_NAME = 'bingo-master-pro-v29'` e NUNCA foi atualizado
nos deploys. O service worker antigo ficava ativo e servia o `network.js` do cache v29
(versão anterior às correções de exclusão). Mesmo sendo network-first, o SW em cache
impedia o `network.js` novo de ser usado de fato no navegador do usuário.

Correção:
- `sw.js`: `CACHE_NAME` v29 -> v30 (no `activate` ele limpa todos os caches antigos).
- `index.html`: registro do SW mudou para `sw.js?v=30` (cache-busting do próprio SW).
- Deploy `328395e`. Agora o navegador deve baixar o `network.js` novo e a exclusão funcionar.
- Lição: toda alteração de frontend exige atualizar o `CACHE_NAME` do sw.js OU versionar o SW.

## 5. Observações / pendências
- Durante os testes em produção, a usuária "Marília" foi excluída no banco real (Neon).
  São 7 usuários reais no Neon (arquivo local `usuarios.json` tem só 2 — é só espelho/local).
- API_BASE aponta para `https://bingo-master-pro-fcty.onrender.com` (produção).
- Anti-fraude: 4 contas (xunda, loxa, Marcos, Marília) compartilham o mesmo fingerprint
  `fp_o5e0cn` = mesmo dispositivo. Não é bug, é o alerta de multi-conta funcionando.
- Sempre que mexer no frontend, atualizar também o `CACHE_NAME` do `sw.js` para evitar
  cache fantasma no navegador do usuário.
